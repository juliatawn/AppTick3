<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
<!--    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="AppTick"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTick">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="AppTick"
            android:theme="@style/Theme.AppTick">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        <activity android:name=".newAppLimit.TabTimeLimitActivity" />-->

<!--        <activity android:name=".premiumMode.PremiumModeActivity" />-->
<!--        <activity android:name=".appLimit.AppLimitDetailsActivity" />-->
        <activity
            android:name=".block.BlockWindowActivity"
            android:taskAffinity="com.juliacai.apptick.block"
            android:excludeFromRecents="true"
            android:noHistory="true" />
        <activity android:name=".deviceApps.AppSearchActivity" />
        <activity android:name=".lockModes.EnterPasswordActivity" />
        <activity android:name=".lockModes.EnterSecurityKeyActivity" />
        <activity android:name=".lockModes.UsbSecurityKeySetupActivity" />
        <activity android:name=".lockModes.SetPassword" />

        <activity android:name=".premiumMode.LockdownModeActivity" />
        <activity android:name=".premiumMode.LockdownTimeActivity" />
        <activity android:name=".deviceApps.GroupPage" />

<!--        <service-->
<!--            android:name=".backgroundProcesses.BackgroundChecker"-->
<!--            android:enabled="true"-->
<!--            android:exported="false" />-->

        <service android:name=".backgroundProcesses.BackgroundChecker" android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Checks what app you are using so your app limit can block it when appropriate"/>
        </service>

        <service
            android:name=".backgroundProcesses.FloatingBubbleService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Shows floating time-remaining bubble overlay while user is in a time-limited app"/>
        </service>

        <receiver
            android:name=".Receiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".premiumMode.DeviceAdmin"
            android:description="@string/app_name"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
